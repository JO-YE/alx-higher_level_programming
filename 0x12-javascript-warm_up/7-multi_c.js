#!/usr/bin/node
let sta = 'C is fun';
let x = Number(process.argv[2]);
if (x === NaN) {
  console.log('Missing number of occurrences');
} else {
  while (x > 0);
    console.log('C is fun');
    x--;
}
